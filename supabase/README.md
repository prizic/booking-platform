# Platform-owned Supabase tree

This platform-only directory will contain migrations, database tests, Edge
Functions, and synthetic local seeds beginning with issue #4. It is reserved
now so the source/distribution ownership boundary can be enforced from the
first monorepo commit.

No production migration or privileged database material belongs in a generated
instance repository.
